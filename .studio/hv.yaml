manager:
  commands:
    start: [sh, /app/.studio/scripts/start.sh]
    healthcheck:
      command: [sh, /app/.studio/scripts/healthcheck.sh]
      interval: 4s
      max_retries: 20
      timeout: 4s
  git:
    repo_dir: /app/repo
    dev_branch: dev
log:
  level: debug
